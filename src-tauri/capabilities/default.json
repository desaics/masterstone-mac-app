{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window. Session 5 adds the opener plugin's default permission set. The frontend doesn't call opener APIs directly — it routes through custom Rust commands (open_external_url, reveal_onedrive_folder) which internally use the OpenerExt trait — so no per-URL or per-path scopes are needed in this capability file.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
