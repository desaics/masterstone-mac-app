{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window. Session 4 adds dialog access for the first-launch DB picker.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
